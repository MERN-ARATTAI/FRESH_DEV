# Admin Frontend

This project expects the backend base URL to be provided via the `VITE_API_BASE_URL` environment variable.

Example `.env` (local development):

VITE_API_BASE_URL="http://localhost:8080/api"

Restart the dev server after changing `.env`.
