import React from 'react'
import Product from '../Components/Product'

const ProductsPage = () => {
  // Render the integrated Product component in page mode
  return <Product pageMode={true} />
}

export default ProductsPage

